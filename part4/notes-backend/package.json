{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon --env-file=.env.local index.js",
    "test": "NODE_ENV=test node --env-file=.env.local --test",
    "lint": "eslint .",
    "build:ui": "rm -rf dist && cd ../../part2/notes && npm install && npm run build && cp -r dist ../../part3/notes-backend",
    "deploy:full": "npm run build:ui && npm install"
  },
  "keywords": [],
  "author": "Josh Javier",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.1.3"
  },
  "devDependencies": {
    "@stylistic/eslint-plugin": "^1.6.2",
    "eslint": "^8.56.0",
    "nodemon": "^3.0.3"
  }
}
